cubes:
    name: faculty_att_slot

    sql_table: odsmgr.faculty_att_slot

    joins: []

    dimensions:
    -   name: person_uid
        sql: person_uid
        type: string

    -   name: profile_code
        sql: profile_code
        type: string

    -   name: process_group
        sql: process_group
        type: string

    -   name: multi_source_desc
        sql: multi_source_desc
        type: string

    -   name: multi_source
        sql: multi_source
        type: string

    -   name: faculty_attribute_desc5
        sql: faculty_attribute_desc5
        type: string

    -   name: faculty_attribute_desc4
        sql: faculty_attribute_desc4
        type: string

    -   name: faculty_attribute_desc3
        sql: faculty_attribute_desc3
        type: string

    -   name: faculty_attribute_desc2
        sql: faculty_attribute_desc2
        type: string

    -   name: faculty_attribute_desc1
        sql: faculty_attribute_desc1
        type: string

    -   name: faculty_attribute5
        sql: faculty_attribute5
        type: string

    -   name: faculty_attribute4
        sql: faculty_attribute4
        type: string

    -   name: faculty_attribute3
        sql: faculty_attribute3
        type: string

    -   name: faculty_attribute2
        sql: faculty_attribute2
        type: string

    -   name: faculty_attribute1
        sql: faculty_attribute1
        type: string

    -   name: attribute_rule
        sql: attribute_rule
        type: string

    -   name: administrative_group
        sql: administrative_group
        type: string

    -   name: activity_date
        sql: activity_date
        type: time

    -   name: academic_period
        sql: academic_period
        type: string

    -   name: academic_period_desc
        sql: academic_period_desc
        type: string

    measures: []

    pre_aggregations: []
