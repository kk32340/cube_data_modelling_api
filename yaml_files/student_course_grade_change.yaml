cubes:
    name: student_course_grade_change

    sql_table: odsmgr.student_course_grade_change

    joins: []

    dimensions:
    -   name: sub_academic_period_desc
        sql: sub_academic_period_desc
        type: string

    -   name: sub_academic_period
        sql: sub_academic_period
        type: string

    -   name: subject_desc
        sql: subject_desc
        type: string

    -   name: subject
        sql: subject
        type: string

    -   name: quality_points
        sql: quality_points
        type: string

    -   name: person_uid
        sql: person_uid
        type: string

    -   name: process_group
        sql: process_group
        type: string

    -   name: name
        sql: name
        type: string

    -   name: multi_source_desc
        sql: multi_source_desc
        type: string

    -   name: multi_source
        sql: multi_source
        type: string

    -   name: incomplete_ext_date
        sql: incomplete_ext_date
        type: time

    -   name: id
        sql: id
        type: string

    -   name: grade_type_desc
        sql: grade_type_desc
        type: string

    -   name: grade_type
        sql: grade_type
        type: string

    -   name: grade_comment_desc
        sql: grade_comment_desc
        type: string

    -   name: grade_comment
        sql: grade_comment
        type: string

    -   name: grade_change_reason_desc
        sql: grade_change_reason_desc
        type: string

    -   name: grade_change_reason
        sql: grade_change_reason
        type: string

    -   name: grade_change_date
        sql: grade_change_date
        type: time

    -   name: final_grade_sequence_number
        sql: final_grade_sequence_number
        type: string

    -   name: final_grade
        sql: final_grade
        type: string

    -   name: credits_passed
        sql: credits_passed
        type: string

    -   name: credits_for_gpa
        sql: credits_for_gpa
        type: string

    -   name: credits_earned
        sql: credits_earned
        type: string

    -   name: credits_attempted
        sql: credits_attempted
        type: string

    -   name: course_sequence_number
        sql: course_sequence_number
        type: string

    -   name: course_reference_number
        sql: course_reference_number
        type: string

    -   name: course_number
        sql: course_number
        type: string

    -   name: course_identification
        sql: course_identification
        type: string

    -   name: administrative_group
        sql: administrative_group
        type: string

    -   name: academic_year_desc
        sql: academic_year_desc
        type: string

    -   name: academic_year
        sql: academic_year
        type: string

    -   name: academic_period_desc
        sql: academic_period_desc
        type: string

    -   name: academic_period
        sql: academic_period
        type: string

    measures: []

    pre_aggregations: []
