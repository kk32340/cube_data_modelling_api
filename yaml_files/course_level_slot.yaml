cubes:
    name: course_level_slot

    sql_table: odsmgr.course_level_slot

    joins: []

    dimensions:
    -   name: subject
        sql: subject
        type: string

    -   name: process_group
        sql: process_group
        type: string

    -   name: multi_source_desc
        sql: multi_source_desc
        type: string

    -   name: multi_source
        sql: multi_source
        type: string

    -   name: course_number
        sql: course_number
        type: string

    -   name: course_level_desc4
        sql: course_level_desc4
        type: string

    -   name: course_level_desc3
        sql: course_level_desc3
        type: string

    -   name: course_level_desc2
        sql: course_level_desc2
        type: string

    -   name: course_level_desc1
        sql: course_level_desc1
        type: string

    -   name: course_level4
        sql: course_level4
        type: string

    -   name: course_level3
        sql: course_level3
        type: string

    -   name: course_level2
        sql: course_level2
        type: string

    -   name: course_level1
        sql: course_level1
        type: string

    -   name: administrative_group
        sql: administrative_group
        type: string

    -   name: activity_date
        sql: activity_date
        type: time

    -   name: academic_period
        sql: academic_period
        type: string

    -   name: academic_period_desc
        sql: academic_period_desc
        type: string

    measures: []

    pre_aggregations: []
