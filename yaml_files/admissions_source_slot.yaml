cubes:
    name: admissions_source_slot

    sql_table: odsmgr.admissions_source_slot

    joins: []

    dimensions:
    -   name: person_uid
        sql: person_uid
        type: string

    -   name: profile_code
        sql: profile_code
        type: string

    -   name: process_group
        sql: process_group
        type: string

    -   name: multi_source_desc
        sql: multi_source_desc
        type: string

    -   name: multi_source
        sql: multi_source
        type: string

    -   name: institution_desc3
        sql: institution_desc3
        type: string

    -   name: institution_desc2
        sql: institution_desc2
        type: string

    -   name: institution_desc1
        sql: institution_desc1
        type: string

    -   name: institution3
        sql: institution3
        type: string

    -   name: institution2
        sql: institution2
        type: string

    -   name: institution1
        sql: institution1
        type: string

    -   name: application_number
        sql: application_number
        type: string

    -   name: admissions_source_rule
        sql: admissions_source_rule
        type: string

    -   name: administrative_group
        sql: administrative_group
        type: string

    -   name: activity_date
        sql: activity_date
        type: time

    -   name: academic_period
        sql: academic_period
        type: string

    measures: []

    pre_aggregations: []
