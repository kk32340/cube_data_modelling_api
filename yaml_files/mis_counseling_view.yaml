cubes:
    name: mis_counseling_view

    sql_table: odsmgr_cust.mis_counseling_view

    joins: []

    dimensions:
    -   name: service
        sql: service
        type: string

    -   name: high_school_code_desc
        sql: high_school_code_desc
        type: string

    -   name: cell_phone
        sql: cell_phone
        type: string

    -   name: day_phone
        sql: day_phone
        type: string

    -   name: email_address
        sql: email_address
        type: string

    -   name: middle_name
        sql: middle_name
        type: string

    -   name: first_name
        sql: first_name
        type: string

    -   name: last_name
        sql: last_name
        type: string

    -   name: id
        sql: id
        type: string

    -   name: college
        sql: college
        type: string

    -   name: svrcnad_ssst_code
        sql: svrcnad_ssst_code
        type: string

    -   name: svrcnad_pidm
        sql: svrcnad_pidm
        type: string

    -   name: term
        sql: term
        type: string

    -   name: college_code
        sql: college_code
        type: string

    -   name: svrcnad_user_id
        sql: svrcnad_user_id
        type: string

    measures: []

    pre_aggregations: []
