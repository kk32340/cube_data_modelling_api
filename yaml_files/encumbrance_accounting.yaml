cubes:
    name: encumbrance_accounting

    sql_table: odsmgr.encumbrance_accounting

    joins:
    -   name: encumbrance
        sql: '{CUBE}.encumbrance_number = encumbrance.encumbrance_number'
        relationship: one_to_many

    dimensions:
    -   name: labor_item_type
        sql: labor_item_type
        type: string

    -   name: sequence_number
        sql: sequence_number
        type: string

    -   name: hr_encumbrance_ind
        sql: hr_encumbrance_ind
        type: string

    -   name: hr_encumbrance_period
        sql: hr_encumbrance_period
        type: string

    -   name: hr_encumbrance_period_desc
        sql: hr_encumbrance_period_desc
        type: string

    -   name: multi_year_encumbrance_ind
        sql: multi_year_encumbrance_ind
        type: string

    -   name: project
        sql: project
        type: string

    -   name: program_desc
        sql: program_desc
        type: string

    -   name: program
        sql: program
        type: string

    -   name: pending_amount
        sql: pending_amount
        type: string

    -   name: process_group
        sql: process_group
        type: string

    -   name: postable_ind
        sql: postable_ind
        type: string

    -   name: program_status
        sql: program_status
        type: string

    -   name: organization_desc
        sql: organization_desc
        type: string

    -   name: organization_code
        sql: organization_code
        type: string

    -   name: location_status
        sql: location_status
        type: string

    -   name: location_desc
        sql: location_desc
        type: string

    -   name: location
        sql: location
        type: string

    -   name: multi_source_desc
        sql: multi_source_desc
        type: string

    -   name: ledger_encumbered_amount
        sql: ledger_encumbered_amount
        type: string

    -   name: ledger_remaining_balance
        sql: ledger_remaining_balance
        type: string

    -   name: commitment_type
        sql: commitment_type
        type: string

    -   name: commitment_type_desc
        sql: commitment_type_desc
        type: string

    -   name: commitment_percent
        sql: commitment_percent
        type: string

    -   name: rule_class
        sql: rule_class
        type: string

    -   name: rule_class_desc
        sql: rule_class_desc
        type: string

    -   name: field_code
        sql: field_code
        type: string

    -   name: field_code_desc
        sql: field_code_desc
        type: string

    -   name: available_balance_override_ind
        sql: available_balance_override_ind
        type: string

    -   name: enc_accounting_user_id
        sql: enc_accounting_user_id
        type: string

    -   name: enc_accounting_activity_date
        sql: enc_accounting_activity_date
        type: time

    -   name: enc_accounting_data_origin
        sql: enc_accounting_data_origin
        type: string

    -   name: enc_accounting_create_date
        sql: enc_accounting_create_date
        type: time

    -   name: enc_accounting_create_user
        sql: enc_accounting_create_user
        type: string

    -   name: enc_accounting_create_source
        sql: enc_accounting_create_source
        type: string

    -   name: multi_source
        sql: multi_source
        type: string

    -   name: item
        sql: item
        type: string

    -   name: organization_status
        sql: organization_status
        type: string

    -   name: fund_status
        sql: fund_status
        type: string

    -   name: fund_desc
        sql: fund_desc
        type: string

    -   name: account_index
        sql: account_index
        type: string

    -   name: account_index_desc
        sql: account_index_desc
        type: string

    -   name: account_index_status
        sql: account_index_status
        type: string

    -   name: fund
        sql: fund
        type: string

    -   name: fiscal_year
        sql: fiscal_year
        type: string

    -   name: fiscal_period
        sql: fiscal_period
        type: string

    -   name: document_encumbrance_status
        sql: document_encumbrance_status
        type: string

    -   name: distribution_percent
        sql: distribution_percent
        type: string

    -   name: encumbrance_status
        sql: encumbrance_status
        type: string

    -   name: encumbrance_number
        sql: encumbrance_number
        type: string
        primary_key: true

    -   name: chart_of_accounts_desc
        sql: chart_of_accounts_desc
        type: string

    -   name: chart_of_accounts
        sql: chart_of_accounts
        type: string

    -   name: chart_of_accounts_status
        sql: chart_of_accounts_status
        type: string

    -   name: administrative_group
        sql: administrative_group
        type: string

    -   name: activity
        sql: activity
        type: string

    -   name: activity_status
        sql: activity_status
        type: string

    -   name: activity_desc
        sql: activity_desc
        type: string

    -   name: account_desc
        sql: account_desc
        type: string

    -   name: account_status
        sql: account_status
        type: string

    -   name: account
        sql: account
        type: string

    measures: []

    pre_aggregations: []
