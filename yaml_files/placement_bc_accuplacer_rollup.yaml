cubes:
    name: placement_bc_accuplacer_rollup

    sql_table: odsmgr_cust.placement_bc_accuplacer_rollup

    joins: []

    dimensions:
    -   name: test_score
        sql: test_score
        type: string

    -   name: accuplacer_test
        sql: accuplacer_test
        type: string

    -   name: place_range
        sql: place_range
        type: string

    -   name: place_level
        sql: place_level
        type: string

    -   name: test_date
        sql: test_date
        type: time

    -   name: test_desc
        sql: test_desc
        type: string

    -   name: test
        sql: test
        type: string

    -   name: id
        sql: id
        type: string

    -   name: person_uid
        sql: person_uid
        type: string

    -   name: test_cat
        sql: test_cat
        type: string

    measures: []

    pre_aggregations: []
