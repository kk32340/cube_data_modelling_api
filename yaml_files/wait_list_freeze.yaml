cubes:
    name: wait_list_freeze

    sql_table: odsmgr_cust.wait_list_freeze

    joins: []

    dimensions:
    -   name: wait_count
        sql: wait_count
        type: string

    -   name: wait_capacity
        sql: wait_capacity
        type: string

    -   name: wait_avail
        sql: wait_avail
        type: string

    -   name: status
        sql: status
        type: string

    -   name: seats_avail
        sql: seats_avail
        type: string

    -   name: max_enrl
        sql: max_enrl
        type: string

    -   name: freeze_date
        sql: freeze_date
        type: time

    -   name: event_desc
        sql: event_desc
        type: string

    -   name: event_date
        sql: event_date
        type: time

    -   name: event
        sql: event
        type: string

    -   name: census_enrl
        sql: census_enrl
        type: string

    -   name: campus
        sql: campus
        type: string

    -   name: crn
        sql: crn
        type: string

    -   name: academic_period
        sql: academic_period
        type: string

    measures: []

    pre_aggregations: []
