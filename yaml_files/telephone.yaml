cubes:
    name: telephone

    sql_table: odsmgr.telephone

    joins:
    -   name: telephone_stusta
        sql: '{CUBE}.id = telephone_stusta.id'
        relationship: one_to_one

    dimensions:
    -   name: phone_ext
        sql: phone_ext
        type: string

    -   name: phone_comment
        sql: phone_comment
        type: string

    -   name: phone_area
        sql: phone_area
        type: string

    -   name: process_group
        sql: process_group
        type: string

    -   name: country_code
        sql: country_code
        type: string

    -   name: phone_unlist_ind
        sql: phone_unlist_ind
        type: string

    -   name: phone_type_desc
        sql: phone_type_desc
        type: string

    -   name: phone_type
        sql: phone_type
        type: string

    -   name: phone_status_ind
        sql: phone_status_ind
        type: string

    -   name: phone_seq_number
        sql: phone_seq_number
        type: string

    -   name: phone_primary_ind
        sql: phone_primary_ind
        type: string

    -   name: phone_number_combined
        sql: phone_number_combined
        type: string

    -   name: phone_number
        sql: phone_number
        type: string

    -   name: phone_intl_access
        sql: phone_intl_access
        type: string

    -   name: name
        sql: name
        type: string

    -   name: multi_source_desc
        sql: multi_source_desc
        type: string

    -   name: multi_source
        sql: multi_source
        type: string

    -   name: id
        sql: id
        type: string
        primary_key: true

    -   name: entity_uid
        sql: entity_uid
        type: string

    -   name: administrative_group
        sql: administrative_group
        type: string

    -   name: address_type_desc
        sql: address_type_desc
        type: string

    -   name: address_type
        sql: address_type
        type: string

    -   name: address_number
        sql: address_number
        type: string

    measures: []

    pre_aggregations: []
